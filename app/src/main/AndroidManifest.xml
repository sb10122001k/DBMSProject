<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datachess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataChess">
        <activity
            android:name=".Result"
            android:label="@string/title_activity_result"
            android:theme="@style/Theme.DataChess.NoActionBar"></activity>
        <activity android:name=".playeInfo" />
        <activity android:name=".addPlayer" />
        <activity android:name=".playerList" />
        <activity android:name=".matchRecord" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FirstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>